meta {
  name: createPet3
  type: http
  seq: 4
}

post {
  url: http://localhost:9812/pets
  body: json
  auth: inherit
}

body:json {
  {"name": "Chris", "species": "blob"}
}

settings {
  encodeUrl: true
  timeout: 0
}
